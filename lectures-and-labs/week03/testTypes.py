# Lab 3.1.1 
# Checking the types of variables
# Author: Daria Sep

# Create 5 variables, one for each type 
# (integer, float, boolean, string, list)
i = 8
fl = 8.5
ready = True
memo = "All the variables"
seasons = ["summer", "autumn", "winter", "spring"]

# Use the type() function to check the type of variables
print ('variable {} is of type {} and value {}'.format( 'i', type(i), i ))
print ('variable {} is of type {} and value {}'.format( 'fl', type(fl), fl ))
print ('variable {} is of type {} and value {}'.format( 'ready', type(ready), ready ))
print ('variable {} is of type {} and value {}'.format( 'memo', type(memo), memo))
print ('variable {} is of type {} and value {}'.format( 'seasons', type(seasons), seasons))
